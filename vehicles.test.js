/*
  Tests for the Vehicle and Car Classes

      Object-Oriented Programming Practice
      Unit #12 of Springboard SWE Bootcamp 
      Solution by José Delpino
*/

describe("Tests for Vehicle", () => {
  it("", function () {
    // validInputCases.forEach(function (testCase) {
    //   const input = testCase[0];
    //   const expectedResult = testCase[1].expectedResult;
    //   expect(calculateMonthlyPayment(input)).toHaveTwoDecimalPlaces();
    // });
  });
  it("", function () {
    // validInputCases.forEach(function (testCase) {
    //   const input = testCase[0];
    //   const expectedResult = testCase[1].expectedResult;
    //   expect(calculateMonthlyPayment(input)).toBe(expectedResult);
  });
});
